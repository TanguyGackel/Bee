using System.Net;
using System.Net.Sockets;
using Microsoft.Data.SqlClient;
using MS_Lib;

internal class Program
{
    static void Main(string[] args)
    {
        // string str = "Data Source=(local); Initial Catalog=BeeDB; Integrated Security=SSPI; Encrypt=false";
        // SqlConnection conn = new SqlConnection(str);
        //
        // DatabaseConnector db = DatabaseConnector.Instance;
        // db.Source = "local";
        // db.Type = ConnectionType.WindowsAuthentication;
        // db.Credentials = "";
        // db.DB = "BeeDB";
        //
        // conn.Open();
        
        // qs.Connection.Open();


        // Socket socket = new Socket(AddressFamily.InterNetwork, SocketType.Stream, ProtocolType.Tcp);
        // IPEndPoint ipEndPoint = new(IPAddress.Parse("127.0.0.1"), 9999);
        // socket.Connect(ipEndPoint);
        //
        // byte[] messageBytes = "Hello World"u8.ToArray();
        //
        // socket.Send(messageBytes);

    }
}